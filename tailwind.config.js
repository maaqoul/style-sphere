/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // Adjust this pattern to match the file types you use (e.g., JSX, JS if you're using Tailwind with React)
  ],

  theme: {
    extend: {},
  },
  plugins: [],
};
